name: readthedosc
channels:
  - defaults
  - pytorch
  - rdkit
dependencies:
  - python=3.7
  - pandas
  - seaborn
  - requests
  - rdkit
  - scikit-learn
  - scipy
  - pytorch-cpu
  - pip:
      - sphinx-autodoc-typehints
      - ruamel.yaml
      - pymatgen
      - tqdm
