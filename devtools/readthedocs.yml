name: readthedosc
channels:
  - defaults
  - pytorch
  - rdkit
dependencies:
  - python=3.6
  - pandas
  - seaborn
  - requests
  - rdkit
  - scikit-learn
  - scipy
  - pytorch-cpu
  - pip:
      - ruamel.yaml
      - pymatgen
